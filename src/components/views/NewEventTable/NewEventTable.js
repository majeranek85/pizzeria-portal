import React from 'react';
import styles from './NewEventTable.module.scss';

const NewEventTable = () => (
  <div className={styles.component}>
    <h2>NewEventTable view</h2>
  </div>
);

export default NewEventTable;
